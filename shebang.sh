
   
    #! /bin/bash
echo "shebang "/"#!"/" is the symbol used to specify the interpretor used for the command.
the hash is a symbol from c sharp and the ! is called the bang.
fo ex.
	if we want to run the script or the file commands then we use the bash path where the bash shell is stored
	since i want to run my  script in bash, so i will specify the path of the bash intepreter in the path above.
	
	command be:
	#! /bin/bash
	"
